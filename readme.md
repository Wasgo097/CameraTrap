# About Project

## ToDo Client

```
[] Add configurable cuda processing into project (frame should be a wrapper on mat and gpumat )
[] Export module should contain both data export to the local drive and data sending to the server (communication submodule?)
[] VideoSource observer and observable 
[] Multithreading (frame producer and consumer (Observer pattern?))
[] Manager which connect all managers
[] Refactoring AppClient constructor (connect with previous task?) 
```

## ToDo Server
